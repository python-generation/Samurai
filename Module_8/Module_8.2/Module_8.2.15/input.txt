# INPUT DATA:

# TEST_1:
docstring = '''Модуль math предоставляет доступ к базовым математическим функциям и константам, включая операции над числами с плавающей точкой, вычисление тригонометрических, экспоненциальных и логарифмических функций, работу с факториалами, округлением и извлечением квадратных корней, а также содержит предопределённые значения таких констант, как число π и основание натурального логарифма e, что делает его удобным инструментом для выполнения стандартных вычислений без необходимости ручной реализации соответствующих алгоритмов.'''

print(make_doc_according_pep_8(docstring))

# TEST_2:
docstring = '''Это очень длинная строка документации, которая должна быть разбита на 
несколько строк, чтобы соответствовать стандарту PEP-8, каждая строка 
не должна превышать 72 символа.'''

print(make_doc_according_pep_8(docstring))

# TEST_3:
docstring = ''''''

print(make_doc_according_pep_8(docstring))

# TEST_4:
docstring = '''Документация'''

print(make_doc_according_pep_8(docstring))

# TEST_5:
docstring = 'a' * 72

print(make_doc_according_pep_8(docstring))

# TEST_6:
docstring = 'a' * 73

print(make_doc_according_pep_8(docstring))

